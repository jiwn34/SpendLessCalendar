<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/nav_graph"
    app:startDestination="@id/calendarFragment">

    <!-- 📅 Calendar 화면 -->
    <fragment
        android:id="@+id/calendarFragment"
        android:name="com.spendless.calendar.ui.calendar.CalendarFragment"
        android:label="CalendarFragment">

        <!-- 날짜 클릭 → 입력 화면 -->
        <action
            android:id="@+id/action_calendar_to_input"
            app:destination="@id/inputFragment" />

        <!-- 날짜 클릭 → 일별 리스트 화면 -->
        <action
            android:id="@+id/action_calendar_to_dailyList"
            app:destination="@id/dailyListFragment" />
    </fragment>

    <!-- ✏️ 지출 입력 화면 -->
    <fragment
        android:id="@+id/inputFragment"
        android:name="com.spendless.calendar.ui.input.InputFragment"
        android:label="InputFragment">

        <argument
            android:name="selectedDate"
            app:argType="string" />
    </fragment>

    <!-- 📋 일별 지출 리스트 화면 -->
    <fragment
        android:id="@+id/dailyListFragment"
        android:name="com.spendless.calendar.ui.daily.DailyListFragment"
        android:label="DailyListFragment">

        <argument
            android:name="selectedDate"
            app:argType="string" />
    </fragment>

    <!-- 📊 리포트 화면 -->
    <fragment
        android:id="@+id/reportFragment"
        android:name="com.spendless.calendar.ui.report.ReportFragment"
        android:label="ReportFragment" />

</navigation>
